type ContractProduct {
    contract: Contract
    product: Product
    remark: String
    count: Int
    productAmount: Int
}

input ContractProductInput {
    productId: ID!
    remark: String
    count: Int
    productAmount: Int
}

input EditContractProductInput {
    productId: ID
    remark: String
    count: Int
    productAmount: Int
}