type ContractProduct {
    contract: Contract
    product: Product
    remark: String
    count: Int
    productAmount: Int
}

input ContractProductInput {
    productId: String!
    remark: String
    count: Int
    productAmount: Int
}